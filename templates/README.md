vueui-<%= module %> ```v0.1.0```
================================

<%= description %>

### Installation
```bash
npm install --save vueui-<%= module %>
```

### Usage
```js
var Vue = require('vue');
var <%= module %> = require('vueui-<%= module %>');

Vue.use(<%= module %>)
```